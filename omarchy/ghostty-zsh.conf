# Interactive terminal shell only; do not change the account's login shell.
command = /usr/bin/zsh

# Keep a solid black background regardless of the selected Omarchy theme.
background = #000000
background-opacity = 1
